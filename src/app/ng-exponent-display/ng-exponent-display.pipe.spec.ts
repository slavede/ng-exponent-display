import { NgExponentDisplayPipe } from './ng-exponent-display.pipe';

describe('NgExponentDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new NgExponentDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
